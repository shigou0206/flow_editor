import 'package:flutter/material.dart';

class ThemeConfig {
  // Canvas colors
  static const Color backgroundColor = Colors.white;
  static const Color gridColor = Color(0xFFE0E0E0);

  // Node colors
  static const Color defaultNodeColor = Colors.white;
  static const Color nodeStrokeColor = Colors.black;
  static const Color selectedNodeColor = Color(0xFF2196F3);

  // Edge colors
  static const Color defaultEdgeColor = Colors.black;
  static const Color selectedEdgeColor = Color(0xFF2196F3);

  // Selection colors
  static const Color selectionBoxColor = Color(0x402196F3);
  static const Color selectionBorderColor = Color(0xFF2196F3);
}
