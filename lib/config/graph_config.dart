class GraphConfig {
  // Canvas configuration
  static const double defaultZoom = 1.0;
  static const double minZoom = 0.1;
  static const double maxZoom = 2.0;

  // Node configuration
  static const double defaultNodeWidth = 150.0;
  static const double defaultNodeHeight = 100.0;

  // Edge configuration
  static const double defaultEdgeThickness = 2.0;
  static const double defaultArrowSize = 10.0;

  // Grid configuration
  static const double gridSize = 20.0;
  static const bool showGrid = true;
}
